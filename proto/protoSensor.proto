syntax = "proto3";

service SensorService {
  rpc StreamSensors (Empty) returns (stream SensorData);
}

message SensorData {
  string id = 1;
  float temperature = 2;
  float humidity = 3;
  int64 timestamp = 4;
}

message Empty {}
